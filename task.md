我现在有mineru官方的解析token为：

```
sk-N11mhN8170ZDJ8N0hs7Y23kGL2JKjgxSVJ5zkLinFkOBmObR
```

其文档为`https://mineru.net/apiManage/docs`，我复制为了`mineru.md`文件。
请帮我更新后端代码，使得支持使用官方的api。`config.json` 文件支持通过类型判断是哪个api。
支持本地（目前的版本），`精准解析 API`（需要实现的版本）
